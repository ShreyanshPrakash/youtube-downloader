import React, { FC } from 'react';
import styled from 'styled-components';

const HelpWrapper = styled.div`

`;

const Help: FC<{}> = () => {

    return (
        <HelpWrapper>
            Help
        </HelpWrapper>
    )
}

export default Help;